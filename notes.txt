Must now Topics

1. File System
    - learn about fs.readFile()
    - path module like - path.join
    - __dirname,__filename
    - uncaught exceptions
    - fs.writeFile()
    - fs.rename()
    - fs.appendFile()
    -fsPromises.readFile()
    -fsPromises.appendFile()
    -fsPromises.rename()
    -fs.unlink() --> helps to delete file
    -handling files with stream
    -rs.pipe() ---> handling data stream using pipes
    -fs.mkdir()
    -fs.existsSync(filename,callback) --> checks if file exists
    -fs.rmdir()

2. Understand the difference between callback API and Promise API
    - Callback API makes the code convoluted and contributes to callback hell
    - Promise API makes it way easier to manage the code using async await

3. NPM package
    -nodemon 
    -package - date i date-fns, uuid
    -gitignore
    -npm update
    -npm install

4.Event Emitter

